pipeline {
    agent any
    stages {
        stage('myStage'){
            steps {
                sh 'ls' 
            }
        }
        stage('Build') {
            steps { 
                sh 'ls' 
            }
        }
    }
}
