pipeline {
    agent any
    stages {
        stage('myStage'){
            steps {
                sh 'echo "ls"' 
            }
        }
        stage('Build') {
            steps { 
                sh 'ls' 
            }
        }
    }
}
